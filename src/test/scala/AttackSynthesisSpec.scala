package edu.ucsb.cs.cs162.regex.attack_synthesis

import org.scalatest._
import edu.ucsb.cs.cs162.regex._
import edu.ucsb.cs.cs162.regex.derivative._

class AttackSynthesisSpec extends FlatSpec with Matchers {
  //----------------------------------------------------------------------------
  // Fixtures and helpers.
  // ---------------------------------------------------------------------------

  import Regex._

  //----------------------------------------------------------------------------
  // Tests.
  // ---------------------------------------------------------------------------

  behavior of "matches"

  it should "generate correct attack for language 1" in { pending }

  it should "generate correct attack for language 2" in { pending }

  // more tests...

  it should "should generate no attack for langauge 3" in { pending }

  // more tests...
}
