package edu.ucsb.cs.cs162.regex.derivative

import org.scalatest._
import edu.ucsb.cs.cs162.regex._

class DerivativeAnalysisSpec extends FlatSpec with Matchers {
  //----------------------------------------------------------------------------
  // Fixtures and helpers.
  // ---------------------------------------------------------------------------

  import Regex._

  //----------------------------------------------------------------------------
  // Tests.
  // ---------------------------------------------------------------------------

  behavior of "the analysis"

  it should "be able to conflate similar expressions" in { pending }

  it should "recognize strings in the language 1" in { pending }

  // more tests...

  it should "not recognize strings not in the language 1" in { pending }

  // more tests...
}
